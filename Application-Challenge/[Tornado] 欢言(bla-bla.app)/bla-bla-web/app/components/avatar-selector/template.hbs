{{#each this.avatars as |avatar|}}
  <button
    class="mb-3"
    type="button"
    {{on "click" (fn this.chooseAvatar avatar)}}
  >
    <Avatar
      @character={{avatar}}
      @status={{if (eq this.avatar avatar) "select"}}
    />
  </button>
{{/each}}