// Generated by CoffeeScript 1.12.7
(function() {
  var Hs, root;

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  root.Hs || (root.Hs = {});

  Hs = root.Hs;

  $(function() {
    $(window).on("wheel touchmove", function(evt) {
      var comeinpage, comeinpages, i, len, results;
      console.log(evt);
      comeinpages = $(".comeinpage");
      results = [];
      for (i = 0, len = comeinpages.length; i < len; i++) {
        comeinpage = comeinpages[i];
        results.push((function(comeinpage) {
          var j, len1, nocomein, nocomeins, points, results1;
          points = comeinpage.getBoundingClientRect();
          if (points.top >= 0 || points.bottom >= 0) {
            nocomeins = $(comeinpage).find(".nocomein");
            $(comeinpage).removeClass("comeinpage");
            results1 = [];
            for (j = 0, len1 = nocomeins.length; j < len1; j++) {
              nocomein = nocomeins[j];
              results1.push((function(nocomein) {
                var dom;
                dom = $(nocomein);
                dom.removeClass("nocomein");
                return dom.addClass("comein");
              })(nocomein));
            }
            return results1;
          }
        })(comeinpage));
      }
      return results;
    });
    return $("body").on("click", ".wechat_qrcode_show_btn", function(evt) {
      if ($(".wechat_qrcode").hasClass("hide")) {
        return $(".wechat_qrcode").removeClass("hide");
      } else {
        return $(".wechat_qrcode").addClass("hide");
      }
    });
  });

}).call(this);
